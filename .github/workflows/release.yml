name: release
on:
  push:
    tags:
      - "v*"
permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cmd/gh-iteration
    steps:
      - uses: actions/checkout@v4
      - uses: cli/gh-extension-precompile@v1
